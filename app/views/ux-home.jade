extends include/layouts/default-ux

block header
  header.header
    .row
      .col-sm-8.col-xs-10
        h1.heading Tim Dosé
        p.title User Experience Designer
        .lead!= md(about.blurb)
      .col-sm-4.hidden-xs.portrait
        img(src='/images/illustration/portrait.png')

block body
  .main-content
    section#projects    
      .row.heading
        .col-sm-12
          h2 Work
      .row
        .project-collection
          each project in projects
            .col-sm-4.col-xs-6
              a.project(href='/ux/projects/' + project.name)
                .thumb-wrapper
                  img.thumb(src='/images/projects/' + project.name + '/' + project.image.thumbnail)
                  .screen
                strong.title
                  span=project.title
                  i.fa.fa-chevron-right
                div.blurb=project.blurb

    .between
    section#about
      .row.heading
        .col-xs-12
          h2 About
      .row
        .col-sm-3.hidden-xs.headshot
          img(src="/images/photos/headshot.jpg")
        .col-sm-9
          .content!= md(about.intro)
          a#resumeButton.active.btn.btn-lg.btn-primary(href="/ux/resume") Download my resume
          //- .more
          //-   a(href='/ux/about') Read More 
          //-     span.fa.fa-angle-right
  
  //- .media-query-sniff
    .visible-xs xs
    .visible-sm sm
    .visible-md md
    .visible-lg lg

