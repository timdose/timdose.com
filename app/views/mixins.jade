mixin thumbnail(big, small )
  - if (small == undefined )
    - small = big.replace('.jpg','.th.jpg').replace('.png', '.th.png');
  .thumbnail
    a(href='#{big}', data-lightbox!=attributes.lightboxGroup)
      img(src='#{small}')